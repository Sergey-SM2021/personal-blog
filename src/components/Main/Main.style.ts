import styled from 'styled-components'
import { color, size } from '../../Global.style'

export const MainWrapper = styled.div`
    display:flex;
    flex-direction:column;
    background-color: ${color.primary};
    min-height: 100%;
    padding-left: ${size.sider} `